# Hanya - OpenAI Mode (Local Development)
# Copy this file to .env.local

# Server-side only - NEVER exposed to browser
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Client-side config (no secrets)
VITE_AI_MODE=openai
